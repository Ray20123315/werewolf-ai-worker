# No deployer-owned AI provider secrets are required.
# AI is optional. When a host adds an AI player, that host supplies their own API Key in the browser.
# The key is used transiently for the requested AI turn and is not stored in Durable Object state.
